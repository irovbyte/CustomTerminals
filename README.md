# 🪟 **Windows Terminal — установка и настройка**

## 📸 Превью PowerShell

![Windows Terminal Preview](Assets/powershell-terminal-preview.png)

---

# 🚀 **Установка конфигурации Windows Terminal**

Конфигурацию можно установить двумя способами.

---

# **Вариант 1 — через сам Windows Terminal (рекомендуется)**

1. Открой Windows Terminal  
2. В левом нижнем углу нажми:

**Settings → Open JSON file**

или горячая клавиша:

**Ctrl + , → Open JSON file**

3. Вставь содержимое файла:

```
Windows/settings.json
```

4. Сохрани файл — настройки применятся автоматически.

---

# **Вариант 2 — вручную через Проводник**

1. Открой папку настроек Windows Terminal:

```
"%LOCALAPPDATA%\Packages\Microsoft.WindowsTerminal_8wekyb3d8bbwe\LocalState"
```

2. Замени файл:

```
settings.json
```

на файл из репозитория:

```
Windows/settings.json
```

---

# 🎛 **Что включено в конфигурацию**

## 🎨 Fluent Dark — кастомная тема

- фон: `#0B0B0D`  
- акцент: `#FF4F7B`  
- синий: `#4D6BFF`  
- мягкие неоновые оттенки  

Тема задаётся в блоке:

```json
"colorScheme": "Fluent Dark"
```

---

# 🧊 **Стеклянность (Acrylic)**

Включена по умолчанию:

```json
"useAcrylic": true,
"acrylicOpacity": 0.65
```

Прозрачность:

- `1.0` — полностью непрозрачный  
- `0.0` — полностью прозрачный  

---

# ⌨️ **Горячие клавиши**

| Комбинация | Действие |
|-----------|----------|
| **Ctrl + W** | открыть WSL Ubuntu |
| **Ctrl + P** | открыть PowerShell |
| **Ctrl + E** | выполнить `exit` |
| **Ctrl + C / Ctrl + V** | копировать / вставить |

Хоткеи задаются в блоке:

```json
"keybindings": [
    { "id": "User.newTab.8C9F1904", "keys": "ctrl+w" },
    { "id": "User.newTab.C5CF5A05", "keys": "ctrl+p" },
    { "id": "User.sendInput.5BF89876", "keys": "ctrl=e" }
]
```

---

# 🖼 **Шрифт и стиль**

Используется:

```json
"font": {
    "face": "MesloLGS NF",
    "size": 16
}
```

Иконка профиля:

```json
"icon": "\uf5b0"
```

---

# 🧩 **Добавление своих терминалов и тем**

## Новый профиль

```json
{
    "name": "My Custom Shell",
    "commandline": "C:\\Path\\to\\app.exe",
    "icon": "🔥"
}
```

## Новая тема

Добавляется в блок `"schemes"`.

---

# 🐧 **WSL Ubuntu — Zsh + Powerlevel10k**

## 📌 Важно про стеклянность (Acrylic)

Стеклянный эффект работает **только в Windows Terminal**.  
WSL сам по себе *не может* сделать прозрачность.

Если нужен стеклянный WSL → установи Windows Terminal.  
**[Перейти к установке Windows Terminal](#)**

---

# 📸 Превью WSL Ubuntu

![WSL Ubuntu Preview](Assets/wsl-ubuntu-terminal-preview.png)

---

# 🚀 Установка Zsh + Powerlevel10k

## 1. Установить Zsh, Git, Curl

```sh
sudo apt update
sudo apt install zsh git curl -y
```

## 2. Установить Oh My Zsh

```sh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```

## 3. Установить Powerlevel10k

```sh
git clone --depth=1 https://github.com/romkatv/powerlevel10k.git $HOME/.oh-my-zsh/custom/themes/powerlevel10k
```

---

# ⚡ Установка моего конфига (автоматически)

```sh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/irovbyte/CustomTerminals/main/InstallWslUbuntu.sh)"
```

Скрипт:

- скачает репозиторий  
- установит `.zshrc` и `.p10k.zsh`  
- удалит временные файлы  
- перезапустит Zsh  

---

# 🧩 Что умеет Zsh в моём конфиге

## ⚡ Instant Prompt

```sh
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi
```

---

## 🎨 Powerlevel10k

```sh
[[ -f "$ZSH_CUSTOM_DIR/.p10k.zsh" ]] && source "$ZSH_CUSTOM_DIR/.p10k.zsh"
```

---

## 🔌 Плагины Oh My Zsh

```sh
plugins=(
  git
  zsh-autosuggestions
  zsh-syntax-highlighting
)
```

- **git** — удобные сокращения  
- **autosuggestions** — автоподсказки как в Fish  
- **syntax-highlighting** — подсветка команд  

---

## 🧊 Алиасы

| Команда | Что делает |
|--------|------------|
| **lsa** | Красивый вывод файлов и папок: цвета, размеры, скрытые файлы, сортировка директорий сверху |
| **zshp** | Перезапускает Zsh без закрытия терминала |
| **zshedit** | Открывает файл `~/.zshrc` в VS Code |
| **open** | Открывает Проводник Windows (`open` или `open .` — текущая папка, `open /mnt/c/...` — указанная директория) |
| **update** | Полное обновление Ubuntu (`apt update`, `apt upgrade`, `autoremove`) |

---

# 🧬 В будущем: поддержка других дистрибутивов

Планируется добавить:

- Debian  
- Arch  
- Fedora  
- Kali  
- openSUSE  
